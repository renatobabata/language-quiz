fastapi==0.141.1
uvicorn[standard]==0.32.0
pydantic-settings==2.6.0
google-generativeai==0.8.3
groq==0.11.0
# Pinned below 0.28: that version removed the `proxies` argument from
# httpx.Client, which the groq SDK 0.11.0 still passes internally, causing
# "Client.__init__() got an unexpected keyword argument 'proxies'".
httpx==0.27.2
sqlalchemy==2.0.35
langdetect==1.0.9
