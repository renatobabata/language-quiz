# Copy this file to terraform.tfvars and fill in real values.
# terraform.tfvars must NEVER be committed (already in .gitignore).

gcp_project_id           = "language-quiz-rbabata"
ssh_public_key_path      = "~/.ssh/id_ed25519.pub"
allowed_ssh_source_range = "0.0.0.0/0" # replace with "YOUR_IP/32"
